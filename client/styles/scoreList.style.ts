import { makeStyles } from '@material-ui/core';

const useStylesScoreList = makeStyles({
  card: {
    width: '190px',
  },
  container: {
      width: '100%',
    
  },
  heading: {
    padding: '20px 0'
  }
});

export default useStylesScoreList;
