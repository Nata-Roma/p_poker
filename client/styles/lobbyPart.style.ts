import { makeStyles } from '@material-ui/core';

const useStylesLobbyPart = makeStyles({
  container: {
    padding: '10px',
    boxShadow: '1px 0 3px rgba(0, 0, 0, 0.3)',
  },
  btn: {
    width: '190px',
  },
  mBottom: {
    marginBottom: '20px',
  },
});

export default useStylesLobbyPart;
